// בדיקה עם הקובץ האמיתי שהמשתמש העלתה
const { standardizeText } = require('../standardize-text.js');
const fs = require('fs');

console.log("═══════════════════════════════════════════════════════");
console.log("🧪 בדיקה עם הקובץ האמיתי מ-18:11");
console.log("═══════════════════════════════════════════════════════\n");

// קרא את הקובץ המקורי
const inputFile = './input-18:11-2025-11-05-0.10196077198335995.js';
let fileContent = fs.readFileSync(inputFile, 'utf8');
// הסר את הגרש היחיד בהתחלה והסוף
fileContent = fileContent.trim();
if (fileContent.startsWith("'")) fileContent = fileContent.substring(1);
if (fileContent.endsWith("'")) fileContent = fileContent.substring(0, fileContent.length - 1);
const originalText = fileContent;

console.log("📥 טקסט מקורי:");
console.log("─────────────────────────────────────────────────────");
// הדפס רק את השורות הרלוונטיות
const lines = originalText.split('\n');
lines.forEach((line, idx) => {
    if (idx >= 15 && idx <= 20) {
        console.log(`שורה ${idx + 1}: ${line}`);
    }
});
console.log("");

// הרץ standardization
const cleanedText = standardizeText(originalText);

console.log("🧹 טקסט אחרי ניקוי:");
console.log("─────────────────────────────────────────────────────");
const cleanedLines = cleanedText.split('\n');
cleanedLines.forEach((line, idx) => {
    if (idx >= 15 && idx <= 20) {
        console.log(`שורה ${idx + 1}: ${line}`);
    }
});
console.log("");

// בדיקות ספציפיות
console.log("═══════════════════════════════════════════════════════");
console.log("📊 בדיקות");
console.log("═══════════════════════════════════════════════════════");

// בדיקה 1: עוסק מורשה
if (cleanedText.includes("558-11-7016")) {
    console.log("❌ שגיאה! עוסק מורשה השתנה ל-558-11-7016");
} else if (cleanedText.includes("558117016")) {
    console.log("✅ מצוין! עוסק מורשה נשאר 558117016");
} else {
    console.log("⚠️  עוסק מורשה לא נמצא בטקסט");
}

// בדיקה 2: מספר רכב (צריך להישאר עם מקפים)
if (cleanedText.includes("419-29-702")) {
    console.log("✅ מצוין! מספר רכב נשאר 419-29-702");
} else {
    console.log("⚠️  מספר רכב השתנה או נעלם");
}

// בדיקה 3: ח.פ
if (cleanedText.includes("513-32-7064")) {
    console.log("❌ שגיאה! ח.פ השתנה לפורמט רכב");
} else if (cleanedText.includes("513327064")) {
    console.log("✅ מצוין! ח.פ נשאר 513327064");
} else {
    console.log("⚠️  ח.פ לא נמצא");
}

// בדיקה 4: טלפון
if (cleanedText.includes("0505020057") || cleanedText.includes("050-5020057")) {
    console.log("✅ מצוין! טלפון לא השתנה לפורמט רכב");
} else {
    console.log("⚠️  טלפון השתנה או נעלם");
}

console.log("═══════════════════════════════════════════════════════");
